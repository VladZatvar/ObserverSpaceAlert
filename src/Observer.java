// Інтерфейс Observer описує спостерігача.
// Спостерігач — це об'єкт, який хоче отримувати повідомлення
// про зміну стану іншого об'єкта.
public interface Observer {

    // Метод update() викликається тоді, коли змінюється рівень небезпеки.
    void update(String dangerLevel);
}